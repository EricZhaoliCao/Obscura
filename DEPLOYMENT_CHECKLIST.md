# Vercel 部署检查清单

## 📋 部署前检查

### 文件结构
- [x] `api/index.ts` - Serverless Function 入口
- [x] `vercel.json` - Vercel 配置文件
- [x] `package.json` - 包含 `build:client` 脚本
- [x] `client/` - 前端源代码
- [x] `server/` - 后端业务逻辑

### 配置文件
- [x] `vercel.json` 配置正确
  - buildCommand: `pnpm build:client`
  - outputDirectory: `dist/client`
  - functions runtime: `nodejs20.x`
- [x] `.vercelignore` 排除不必要的文件
- [x] `api/tsconfig.json` TypeScript 配置

### 代码修改
- [x] 移除 OAuth 认证系统
- [x] 移除数据库依赖
- [x] 使用内存存储
- [x] 简化用户认证逻辑

## 🚀 部署步骤

### 1. 推送到 GitHub
```bash
git init
git add .
git commit -m "Initial commit for Vercel deployment"
git branch -M main
git remote add origin https://github.com/你的用户名/obscura.git
git push -u origin main
```

### 2. 连接 Vercel
1. 访问 https://vercel.com
2. 登录并点击 "New Project"
3. 选择 GitHub 仓库
4. 导入项目

### 3. 配置项目
Vercel 会自动检测 `vercel.json`，无需手动配置。

确认以下设置：
- Framework Preset: Other
- Build Command: `pnpm build:client`
- Output Directory: `dist/client`
- Install Command: `pnpm install`

### 4. 部署
点击 "Deploy" 按钮，等待构建完成。

## ✅ 部署后验证

### 前端检查
- [ ] 网站可以访问
- [ ] 首页正常显示
- [ ] 导航菜单工作
- [ ] 样式加载正确
- [ ] 图片资源加载

### API 检查
- [ ] `/api/health` 返回 200
- [ ] tRPC 端点工作
- [ ] 数据可以读取
- [ ] 数据可以写入（内存）

### 功能检查
- [ ] 博客列表显示
- [ ] 文档管理工作
- [ ] 健康记录功能
- [ ] 财务管理功能
- [ ] AI 聊天（如配置）

## 🔧 常见问题

### 构建失败

**错误**: `Command "pnpm build:client" exited with 1`

**解决方案**:
1. 检查 `package.json` 中是否有 `build:client` 脚本
2. 本地运行 `pnpm build:client` 测试
3. 检查 TypeScript 错误：`pnpm check`

**错误**: `Cannot find module`

**解决方案**:
1. 确认所有依赖在 `package.json` 中
2. 删除 `node_modules` 和 `pnpm-lock.yaml`
3. 重新运行 `pnpm install`

### 运行时错误

**错误**: API 返回 500

**解决方案**:
1. 查看 Vercel 函数日志
2. 检查 `api/index.ts` 导入路径
3. 确认 TypeScript 编译成功

**错误**: 冷启动慢

**说明**: 这是 Serverless 的正常现象
- 首次请求需要 1-2 秒
- 后续请求会快很多
- 考虑升级到 Pro 计划

### 数据问题

**问题**: 数据不持久化

**说明**: 当前使用内存存储
- 每个函数调用独立
- 数据不会保存

**解决方案**:
1. 使用 Vercel KV (Redis)
2. 使用 Vercel Postgres
3. 集成第三方数据库

## 📊 性能优化

### 已启用
- [x] Vercel CDN
- [x] 自动压缩
- [x] HTTP/2
- [x] 智能缓存

### 建议优化
- [ ] 配置缓存头
- [ ] 使用 Edge Functions
- [ ] 压缩图片
- [ ] 代码分割

## 🔐 安全检查

- [ ] 环境变量已配置（如需要）
- [ ] API 密钥不在代码中
- [ ] CORS 配置正确
- [ ] 输入验证启用

## 📈 监控

### Vercel Dashboard
- 访问量统计
- 函数执行时间
- 错误日志
- 带宽使用

### 建议工具
- Sentry（错误追踪）
- LogRocket（用户会话）
- Google Analytics（访问统计）

## 🎯 下一步

### 生产就绪
- [ ] 添加真实数据库
- [ ] 实现用户认证
- [ ] 配置自定义域名
- [ ] 设置 SSL 证书
- [ ] 启用监控告警

### 功能增强
- [ ] 添加缓存层
- [ ] 实现文件上传
- [ ] 集成支付系统
- [ ] 添加邮件通知

## 📞 获取帮助

### 文档
- [Vercel 文档](https://vercel.com/docs)
- [Serverless Functions](https://vercel.com/docs/functions)
- [项目 README](./VERCEL_DEPLOYMENT.md)

### 支持
- Vercel 社区论坛
- GitHub Issues
- Discord 社区

---

**检查完成后，开始部署！** 🚀
